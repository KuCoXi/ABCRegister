<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PrintActivity" >
	<TextView
	    android:id="@+id/tvHeadTitle"
	    android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:textStyle="bold"
        android:textSize="20sp"
        android:text="打印回单"/>
    <Button
        android:id="@+id/btOpenBluetooth"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvHeadTitle"
        android:layout_marginTop="5dp"
        android:text="打开蓝牙" />
    <Button
        android:id="@+id/btBluetoothSetting"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btOpenBluetooth"
        android:text="蓝牙打印机设置" />

    <Button
        android:id="@+id/btSubmit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btBluetoothSetting"
        android:text="确认打印" />

</RelativeLayout>